<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <IsPackable>true</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- 本工程为仓级 D3D11 原生互操作底座：仅引用 Abstractions + 原生 P/Invoke（d3d11.dll / dxgi.dll），
         不引 Vortice / SharpGen（其源生成 COM 非真零反射）。AOT 100% 友好：手写 vtable、零反射、零 [ComImport]。 -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Infrastructure\LingFan.Media.Abstractions\LingFan.Media.Abstractions.csproj" />
  </ItemGroup>

</Project>

# vitepress-plugin-mermaid requires hoisted dependencies so its runtime
# bundle can resolve 'mermaid' from the top-level node_modules layout.
# See https://github.com/emersonbottero/vitepress-plugin-mermaid#install
shamefully-hoist=true
